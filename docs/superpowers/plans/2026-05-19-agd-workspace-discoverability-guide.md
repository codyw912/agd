**Goal:** Document the current workspace entry model and the `.agd/default` symlink design criteria.

**Architecture:** Keep this documentation-only. Explain current `agd shell`, `agd path`, and `agd workspace list` usage, then capture the symlink tradeoffs before implementing behavior.

**Steps:**

1. Add a workspace discoverability guide.
2. Link it from README and the next-phase workflow UX track.
3. Run formatting, lint, and tests.
