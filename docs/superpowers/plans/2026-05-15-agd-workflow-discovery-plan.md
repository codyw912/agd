**Goal:** Define the next project phase as workflow discovery before simplification or new feature work.

**Architecture:** Keep this documentation-only. Add a phase plan that explains how observations become roadmap work and an observation log with a reusable template. Do not change command behavior or architecture yet.

**Steps:**

1. Add `docs/next-phase.md` with the workflow discovery process.
2. Add `docs/workflow-discovery.md` with the observation template.
3. Run the normal documentation-safe verification gate.
