**Goal:** Finalize the first alpha release docs so `v0.1.0` can be tagged from `main`.

**Architecture:** Keep this documentation-only. Move the changelog entry from `Unreleased` to the concrete `0.1.0` release and make the release checklist include exact tag, push, and post-tag install verification commands.

**Steps:**

1. Add an empty `Unreleased` changelog section.
2. Move the initial alpha notes under `0.1.0 - 2026-05-15`.
3. Expand the release checklist with exact tagging and post-tag verification steps.
4. Run the standard release gates.
