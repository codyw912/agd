**Goal:** Convert recent workflow discovery into a decision and implementation-candidate snapshot.

**Architecture:** Keep this documentation-only. Add a current decision artifact that distinguishes accepted direction, deferred ideas, and next implementation candidates so future PRs follow the dogfooding evidence instead of adding random surface area.

**Steps:**

1. Add `docs/workflow-decisions.md`.
2. Link it from `docs/next-phase.md` and `README.md`.
3. Run formatting, lint, and tests.
