**Goal:** Record the first workflow discovery observation from real AGD dogfooding.

**Architecture:** Keep this observation-only. Capture the repeated post-merge sync race between the human checkout and managed workspace without implementing a fix yet.

**Steps:**

1. Add the observation to `docs/workflow-discovery.md`.
2. Run documentation-safe verification.
